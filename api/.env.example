APP_NAME="AI Planning Studio"
APP_ENV=local
APP_KEY=
APP_DEBUG=false
APP_URL=http://localhost

DB_CONNECTION=pgsql
DB_HOST=db
DB_PORT=5432
DB_DATABASE=aistack
DB_USERNAME=postgres
DB_PASSWORD=postgres

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_SAME_SITE=lax
SESSION_DOMAIN=localhost
SESSION_SECURE_COOKIE=false

SANCTUM_STATEFUL_DOMAINS=localhost
FRONTEND_URL=http://localhost:4197

# Google OAuth (isi kredensial dari Google Cloud Console)
# Redirect URI yang terdaftar: http://localhost:4197/api/auth/google/callback
# Untuk produksi/Cloudflare pakai: https://<domain>/api/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:4197/api/auth/google/callback

REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=aistack_redis_2024

# Docker Compose overrides (set di .env root project):
# POSTGRES_PASSWORD=postgres
# DB_PASSWORD=postgres

# Sentry/GlitchTip error monitoring (leave empty to disable)
SENTRY_LARAVEL_DSN=
SENTRY_ENVIRONMENT=local
